/* Hacer un ciclo infiito qyue imprima multiplos de 2*/
#include<stdio.h>

main(){
    int mul, i = 1;
    while(i<5200){//el libro pide infinito a ver que pasa
        mul = i * 2;
        printf ("%d", mul);
        i++;
        }
}
